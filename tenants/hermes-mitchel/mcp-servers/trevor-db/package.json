{
  "name": "@overnightdesk/hermes-mitchel-trevor-db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "trevor-db": "dist/index.js"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "test": "npm run build && rm -rf dist-tests && tsc -p tsconfig.tests.json && node --test \"dist-tests/tests/*.test.js\""
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.21.0",
    "pg": "^8.16.3",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.15.6",
    "typescript": "^5.7.0"
  }
}
