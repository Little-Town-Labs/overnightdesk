# Certificate-bootstrap-only source. It never proxies the dashboard upstream.
server {
    listen 80;
    server_name titus-dashboard.overnightdesk.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / { return 404; }
}
